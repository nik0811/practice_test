```
Docker Configuration:   /var/lib/docker/containersDocker authenticate private registry:  ~/.docker/config.json
Docker-compose :  Compose file version 3 reference | Docker Documentation

To run docker compose

cd compose && docker-compose up --build -d

To stop docker container

cd compose && docker-compose down
```
